# python
__pycache__/
*.pyc

# macOS
.DS_Store

# runtime state lives in ~/.claude/statusline/, never inside this repo —
# but ignore these names just in case someone copies state in by mistake
/active
/state.json
/backups/

# private working notes — never publish
SESSION_RECAP*.md
CLAUDE.local.md
notes/
*.local.*
