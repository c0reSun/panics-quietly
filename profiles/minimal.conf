# profile: minimal — чистий, нестрашний. Лише найпотрібніше.
# формат: показник = on/off [параметри]  # пояснення
# (рядки rule:/rules: зарезервовані під автоматизацію v2, рушій їх ігнорує)

glyphs = unicode            # unicode | ascii (ascii — якщо термінал не малює █▒░⏷Δ)

ctx    = on   width=12 tokens   # контекст: бар + використано/ліміт (.11/1)
model  = on                     # яка модель активна
